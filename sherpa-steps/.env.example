# Wix Headless environment variables.
# Do NOT commit real values — run `npx wix env pull` to populate a local .env.local.
WIX_CLOUD_PROVIDER=
WIX_CLIENT_ID=
WIX_CLIENT_INSTANCE_ID=
WIX_CLIENT_PUBLIC_KEY=
WIX_CLIENT_SECRET=
